@model CustomSearchModel

@using ACT.Core.Models

@Html.DropDownListFor( model => model.RegionId, model => model.RegionOptions, r => r.Key, r => r.Value, true, new { @class = "chzn", @data_gs_region = 5, @style = "width: 100%;" } )